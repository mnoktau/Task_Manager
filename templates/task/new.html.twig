{% extends 'base.html.twig' %}

{% block body %}
    <h1>Yeni Görev Ekle</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Kaydet') }}</button>
    {{ form_end(form) }}
{% endblock %}
